---
import BaseLayout from '../layouts/BaseLayout.astro';
import { siteUrl } from '../utils/url';
---

<BaseLayout
  title="Services — AstroDemo"
  description="Professional web services built on Astro: fast static sites, performance audits, accessibility consulting, content architecture, and deployment."
>

  <!-- Page hero -->
  <section class="page-hero" aria-labelledby="services-heading">
    <div class="container">
      <div class="eyebrow">What we offer</div>
      <h1 id="services-heading">Services that put performance first.</h1>
      <p class="lead">
        Every service is built around one principle: ship less, load faster, reach more people.
        We use Astro and modern web standards to deliver sites that score 100 on Lighthouse out of the box.
      </p>
    </div>
  </section>

  <!-- Services grid -->
  <section class="services-section" aria-labelledby="services-grid-heading">
    <div class="container">
      <h2 id="services-grid-heading" class="sr-only">Service list</h2>
      <ul class="services-grid" role="list">

        <li class="service-card">
          <div class="service-icon" aria-hidden="true">
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24"
                 fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/>
            </svg>
          </div>
          <h3>Static Site Development</h3>
          <p>Full design-to-deployment of lightning-fast Astro sites. No server runtime, no cold starts — just pre-rendered HTML served straight from a CDN.</p>
          <ul class="service-features">
            <li>Astro 4 with TypeScript strict mode</li>
            <li>Content Collections for Markdown/MDX</li>
            <li>WCAG 2.1 AA accessibility baked in</li>
            <li>GitHub Actions CI/CD pipeline</li>
          </ul>
        </li>

        <li class="service-card">
          <div class="service-icon" aria-hidden="true">
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24"
                 fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>
            </svg>
          </div>
          <h3>Performance Audit</h3>
          <p>A deep-dive analysis of your existing site's Core Web Vitals, bundle size, render-blocking resources, and image optimisation — with a prioritised action plan.</p>
          <ul class="service-features">
            <li>Lighthouse CI baseline report</li>
            <li>Bundle analysis and tree-shaking review</li>
            <li>Image and font loading strategy</li>
            <li>Before/after measurement</li>
          </ul>
        </li>

        <li class="service-card">
          <div class="service-icon" aria-hidden="true">
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24"
                 fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
              <polyline points="22 4 12 14.01 9 11.01"/>
            </svg>
          </div>
          <h3>Accessibility Consulting</h3>
          <p>WCAG 2.1 AA compliance review covering keyboard navigation, screen-reader compatibility, colour contrast, focus management, and semantic markup.</p>
          <ul class="service-features">
            <li>Manual testing with NVDA and VoiceOver</li>
            <li>Automated axe-core scanning</li>
            <li>Colour contrast verification (4.5:1 minimum)</li>
            <li>Remediation roadmap with code examples</li>
          </ul>
        </li>

        <li class="service-card">
          <div class="service-icon" aria-hidden="true">
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24"
                 fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
              <polyline points="14 2 14 8 20 8"/>
              <line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/>
              <polyline points="10 9 9 9 8 9"/>
            </svg>
          </div>
          <h3>Content Architecture</h3>
          <p>Design a scalable content model using Astro Content Collections and Zod schemas — so your Markdown stays type-safe, validated, and easy to query as the site grows.</p>
          <ul class="service-features">
            <li>Zod schema design and documentation</li>
            <li>Taxonomy and tagging strategy</li>
            <li>MDX component library setup</li>
            <li>Migration from CMS or legacy blog</li>
          </ul>
        </li>

        <li class="service-card">
          <div class="service-icon" aria-hidden="true">
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24"
                 fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
              <line x1="3" y1="9" x2="21" y2="9"/>
              <line x1="9" y1="21" x2="9" y2="9"/>
            </svg>
          </div>
          <h3>UI / Design System</h3>
          <p>A token-based CSS design system built with custom properties — consistent colours, spacing, and typography across every component, with dark mode included.</p>
          <ul class="service-features">
            <li>CSS custom properties token architecture</li>
            <li>Light and dark mode from day one</li>
            <li>Responsive type scale and spacing system</li>
            <li>Component library with Astro + scoped styles</li>
          </ul>
        </li>

        <li class="service-card">
          <div class="service-icon" aria-hidden="true">
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24"
                 fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/>
              <line x1="2" y1="12" x2="22" y2="12"/>
              <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
            </svg>
          </div>
          <h3>Deployment & DevOps</h3>
          <p>Set up a zero-downtime CI/CD pipeline: GitHub Actions builds and type-checks on every push, then deploys automatically to GitHub Pages, Netlify, or Cloudflare.</p>
          <ul class="service-features">
            <li>GitHub Actions workflow authoring</li>
            <li>GitHub Pages, Netlify, or Vercel setup</li>
            <li>Custom domain and HTTPS configuration</li>
            <li>Configurable base path for any hosting</li>
          </ul>
        </li>

      </ul>
    </div>
  </section>

  <!-- Process section -->
  <section class="process-section" aria-labelledby="process-heading">
    <div class="container">
      <h2 id="process-heading" class="section-title">How it works</h2>
      <ol class="process-steps" role="list">
        <li class="step">
          <span class="step-number" aria-hidden="true">01</span>
          <div class="step-body">
            <h3>Discovery call</h3>
            <p>A 30-minute conversation to understand your goals, constraints, and current stack. No sales pitch — just honest scoping.</p>
          </div>
        </li>
        <li class="step">
          <span class="step-number" aria-hidden="true">02</span>
          <div class="step-body">
            <h3>Proposal & scope</h3>
            <p>A written proposal with a clear deliverable list, timeline, and fixed price. No hidden extras, no scope creep.</p>
          </div>
        </li>
        <li class="step">
          <span class="step-number" aria-hidden="true">03</span>
          <div class="step-body">
            <h3>Build & review</h3>
            <p>Iterative development on a feature branch with pull requests. You review and approve each milestone before it ships.</p>
          </div>
        </li>
        <li class="step">
          <span class="step-number" aria-hidden="true">04</span>
          <div class="step-body">
            <h3>Handoff & support</h3>
            <p>Full documentation, a CLAUDE.md for AI-assisted maintenance, and 30 days of free bug-fix support after launch.</p>
          </div>
        </li>
      </ol>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta-section" aria-labelledby="cta-heading">
    <div class="container cta-inner">
      <div>
        <h2 id="cta-heading">Ready to start a project?</h2>
        <p class="cta-desc">Reach out and let's talk about what you need. First response within one business day.</p>
      </div>
      <a href={siteUrl('contact')} class="btn-cta">Get in touch &rarr;</a>
    </div>
  </section>

</BaseLayout>

<style>
  /* ── Page hero ────────────────────────────────────── */
  .page-hero {
    padding-block: var(--space-16);
    border-bottom: 1px solid var(--color-border);
    background: linear-gradient(
      135deg,
      color-mix(in srgb, var(--color-primary) 6%, var(--color-bg)),
      var(--color-bg)
    );
  }

  .eyebrow {
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--color-primary);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: var(--space-4);
  }

  h1 {
    font-size: clamp(var(--text-3xl), 5vw, 3rem);
    letter-spacing: -0.03em;
    margin-bottom: var(--space-6);
  }

  .lead {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
    max-width: 52ch;
    line-height: 1.7;
  }

  /* ── Services grid ────────────────────────────────── */
  .services-section {
    padding-block: var(--space-16);
    border-bottom: 1px solid var(--color-border);
  }

  .sr-only {
    position: absolute;
    width: 1px; height: 1px;
    padding: 0; margin: -1px;
    overflow: hidden;
    clip: rect(0,0,0,0);
    white-space: nowrap;
    border: 0;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: var(--space-6);
    list-style: none;
  }

  .service-card {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    padding: var(--space-8);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    transition: border-color 0.2s, box-shadow 0.2s;
  }

  .service-card:hover {
    border-color: var(--color-primary);
    box-shadow: var(--shadow-md);
  }

  .service-icon {
    color: var(--color-primary);
  }

  .service-card h3 {
    font-size: var(--text-xl);
    font-weight: 700;
    letter-spacing: -0.02em;
  }

  .service-card > p {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    line-height: 1.7;
    flex: 1;
  }

  .service-features {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    border-top: 1px solid var(--color-border);
    padding-top: var(--space-4);
    margin-top: auto;
  }

  .service-features li {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    padding-left: var(--space-4);
    position: relative;
  }

  .service-features li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--color-primary);
    font-weight: 700;
  }

  /* ── Process ──────────────────────────────────────── */
  .process-section {
    padding-block: var(--space-16);
    border-bottom: 1px solid var(--color-border);
  }

  .section-title {
    margin-bottom: var(--space-8);
    letter-spacing: -0.02em;
  }

  .process-steps {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: var(--space-6);
    list-style: none;
  }

  .step {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    padding: var(--space-6);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
  }

  .step-number {
    font-size: var(--text-3xl);
    font-weight: 800;
    color: color-mix(in srgb, var(--color-primary) 25%, var(--color-border));
    letter-spacing: -0.04em;
    line-height: 1;
  }

  .step-body h3 {
    font-size: var(--text-lg);
    font-weight: 600;
    margin-bottom: var(--space-2);
  }

  .step-body p {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    line-height: 1.65;
  }

  /* ── CTA ──────────────────────────────────────────── */
  .cta-section {
    padding-block: var(--space-16);
    background: linear-gradient(
      135deg,
      color-mix(in srgb, var(--color-primary) 8%, var(--color-bg)),
      color-mix(in srgb, var(--color-accent) 5%, var(--color-bg))
    );
  }

  .cta-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-8);
    flex-wrap: wrap;
  }

  .cta-inner h2 {
    font-size: var(--text-2xl);
    margin-bottom: var(--space-2);
    letter-spacing: -0.02em;
  }

  .cta-desc {
    color: var(--color-text-muted);
    font-size: var(--text-base);
  }

  .btn-cta {
    display: inline-flex;
    align-items: center;
    padding: var(--space-4) var(--space-8);
    background: var(--color-primary);
    color: #fff;
    font-weight: 700;
    font-size: var(--text-base);
    border-radius: var(--radius-md);
    text-decoration: none;
    white-space: nowrap;
    flex-shrink: 0;
    transition: background-color 0.15s;
  }

  .btn-cta:hover {
    background: var(--color-primary-hover);
    color: #fff;
    text-decoration: none;
  }
</style>
